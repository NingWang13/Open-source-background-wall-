# Gradle properties - use Windows certificate store + disable SSL verification

org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -Djavax.net.ssl.trustStoreType=WindowsRoot

# Try to bypass SSL issues
systemProp.javax.net.ssl.trustStore=
systemProp.javax.net.ssl.trustStorePassword=
